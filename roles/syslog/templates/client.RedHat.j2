# # multicast
# destination loghost {
# udp("239.2.11.71" port(514));
# };

destination loghost {
{% for host in hostvars %}
udp("{{hostvars[host][ansible_paths[interfaces.system]]['ipv4']['address']}}" port(514)); 
{% endfor %}
};

log { source(s_sys); destination(loghost); };
